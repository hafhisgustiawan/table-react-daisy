const BasicLoading = () => {
  return (
    <div
      className={`grow min-h-screen w-full flex flex-col items-center justify-center`}
    >
      <span className="loading loading-spinner text-primary"></span>
    </div>
  );
};

export default BasicLoading;
